<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="account_invoice.list">
      <field name="name">account_invoice list</field>
      <field name="model">account_invoice.account_invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="account_invoice.action_window">
      <field name="name">account_invoice window</field>
      <field name="res_model">account_invoice.account_invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="account_invoice.action_server">
      <field name="name">account_invoice server</field>
      <field name="model_id" ref="model_account_invoice_account_invoice"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="account_invoice" id="account_invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="account_invoice.menu_1" parent="account_invoice.menu_root"/>
    <menuitem name="Menu 2" id="account_invoice.menu_2" parent="account_invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="account_invoice.menu_1_list" parent="account_invoice.menu_1"
              action="account_invoice.action_window"/>
    <menuitem name="Server to list" id="account_invoice" parent="account_invoice.menu_2"
              action="account_invoice.action_server"/>
    -->
  </data>
</odoo>